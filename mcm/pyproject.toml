[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name            = "model-cache-manager"
version         = "0.1.0"
description     = "Manage indexing, search and more for kernel caches"
authors         = [{name = "Alessandro Sangiorgi", email = "asangior@redhat.com"}]
requires-python = ">=3.9"

dependencies = [
  "rich>=13.7",
  "typer[all]",
  "structlog",
  "pydantic>=2",
  "pydantic-settings>=2",
  "sqlalchemy>=2.0.40",
  "vllm==0.15.1",
]

[project.scripts]
mcm = "model_cache_manager.cli.main:run"

[tool.pylint."messages_control"]
disable = ["import-error"]

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
include = ["model_cache_manager*"]
exclude = ["model_cache_manager/tests"]
